# Y-Lang Compiler

> This is a work in progress and is no where near as optimized or as good as it should be
> I am totally not the best at writing rust or compilers, I'm learning while developing this project.

This is a compiler for a language I'm developing for educational purpose. I'd like to learn more about compilers and compiler optimizations, while also getting better at the Rust programming language and the RISC-V ISA.

Y-Lang is a custom language heavily inspired by many other popular languages, which gets compiled to RISC-V.

Why is it called Y-Lang you may ask? Well, the initial of my first name is "Y". Plus whenever you use this language the first question you should ask yourself is "Why?".

## TODO:
Add static type checking, preferably through some kind of (working) type environment
Add a system to generate an SSA from the AST generated by the parser
Optimize the SSA
- Constant folding & propagation
- Dead code elimination
- Basic loop optimization
- Try instruction scheduling
- Common subexpression elimination
- Make a working register allocator
- Tail call optimization

Instead of turning AST into RISC-V, turn the optimized SSA into RISC-V

## Grammer
Below is the grammer for the language written in **BNF (Backus-Naur Form)**.
```math
\begin{align}
&\langle program \rangle && \Coloneqq &&\langle statement \rangle* \\
\\
&\langle statement \rangle && \Coloneqq &&\langle declaration \rangle \\
&&&&& | \langle assignment \rangle\\
&&&&& | \langle block \rangle\\
&&&&& | \langle expression \rangle\\
\\
&\langle declaration \rangle && \Coloneqq &&"let" \quad \langle identifier \rangle \quad ":" \quad \langle type \rangle \quad "=" \quad \langle expression \rangle \quad ";" \\
\\
&\langle assignment \rangle && \Coloneqq && \langle identifier \rangle \quad "=" \quad \langle expression \rangle \\
\\
&\langle block \rangle && \Coloneqq && "\{" \quad \langle statement \rangle * \quad "\}" \\
\\
&\langle expression \rangle && \Coloneqq && \langle literal \rangle \\
&&&&& | \langle identifier \rangle \\
&&&&& | \langle expression \rangle \quad "+" \quad \langle expression \rangle \\
&&&&& | \langle expression \rangle \quad "-" \quad \langle expression \rangle \\
&&&&& | \langle expression \rangle \quad "*" \quad \langle expression \rangle \\
&&&&& | \langle expression \rangle \quad "/" \quad \langle expression \rangle \\
\\
&\langle parameter-list \rangle && \Coloneqq && \langle parameter \rangle \quad ("," \quad \langle parameter \rangle)*\\
\\
&\langle parameter \rangle && \Coloneqq && \langle identifier \rangle \quad ":" \quad \langle type \rangle \\
\\
&\langle type \rangle && \Coloneqq && "int" | "uint" | "bool" | "string" | "void" \\
&&&&& | "fn" \quad "(" \quad \langle parameter-list \rangle \quad ")" \quad "\text{->}" \quad \langle type \rangle

\end{align}
```